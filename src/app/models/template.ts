export interface Template {
    project: {
        projectId: number,
        projectName: string
    },
    templateId: number,
    templateFormula: string,
    templateName: string,
    templateType: string
}